node_modules/
.DS_Store
*.log
.claude/settings.local.json
_refs/
.agents/*
# Keep the Codex marketplace source tracked (distributable), ignore the rest of .agents/
!.agents/plugins/
.agents/plugins/*
!.agents/plugins/marketplace.json
.claude/
.factory/
skills-lock.json

# Built skill packages and submission archives are local artifacts.
*.zip
/output/chatgpt-skills/
CLAUDE.md
AGENTS.md

# Local-only working files (notes, drafts, builds). Never committed.
/local/
