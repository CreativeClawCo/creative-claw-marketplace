interface:
  display_name: "Creative Claw — Render Video Edits"
  short_description: "Render precise cuts, crop paths, audio, and captions"
  default_prompt: "Use $creativeclaw-render-video-edl to render my chosen video cuts with clean audio, framing, and timed captions."
dependencies:
  tools:
    - type: "mcp"
      value: "creative-claw"
      description: "Creative Claw media editing and asset tools"
      transport: "streamable_http"
      url: "https://app.creativeclaw.co/mcp/chatgpt"
policy:
  allow_implicit_invocation: true
